# dependencies for the documentation (excluding the plugin itself)
# Version pinning is used to prevent accidentially breaking it, you con probably use whatever version you like to use, as long as it is somewhat recent
mkdocs==1.3.1
mkdocs-material==8.3.9
